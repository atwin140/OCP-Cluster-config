apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keycloak

commonAnnotations:
  cluster.name: hub
  cluster.role: management

resources:
- ../../base
- keycloak.yaml
- keycloak-route.yaml
