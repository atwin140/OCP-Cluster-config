apiVersion: v1
kind: Secret
metadata:
  name: keycloak-group-sync
  namespace: group-sync-operator
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  # Replace with a service account or user that has the Keycloak permissions
  # required by group-sync-operator:
  # - query-groups
  # - query-users
  # - view-users
  username: CHANGE_ME
  password: CHANGE_ME
